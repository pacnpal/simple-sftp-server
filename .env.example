# Host port to expose SFTP on
SFTP_HOST_PORT=2222

# Runtime uid/gid mapping for bind mounts (optional)
PUID=1000
PGID=1000

# Paths inside the container
SFTP_PATHS=/data
SSH_KEY_DIR=/home/sftpuser/.ssh
HOST_KEY_DIR=/etc/ssh/host_keys
