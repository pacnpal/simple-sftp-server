# Host port to expose SFTP on
SFTP_HOST_PORT=2222

# Runtime uid/gid mapping for bind mounts (optional)
PUID=1000
PGID=1000

# Paths inside the container
SFTP_PATHS=/data
SSH_KEY_DIR=/keys
HOST_KEY_DIR=/host_keys
# Recommended filesystem jail mode: true requires SYS_CHROOT capability
SFTP_CHROOT=true
